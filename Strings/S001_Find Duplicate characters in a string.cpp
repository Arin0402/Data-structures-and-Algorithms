#include <bits/stdc++.h>
using namespace std;

// 1
// using array of size 256.
// Note - we always takearay of size 256 as there are 256 ascii characters.
// memory wastage

// 2
// using map
// Tc - O(nlogn)
// Sc - O(k)

// 3
// using unordered map
// O(N) (traversal) (map operations take O(1))
// O(k)
